in 服事表管理 , upon copy, edit, or create when I select 成員 and 角色 , if I forgot to click + and click 儲存，
I saved an event without that role.

I find this UX confusing. Let's instead, just render all the roles under that service type. Upon copy or create, leave the assigned member of the role to empty. Upon edit, fill default value with existing ones. Upon save, add the newly added onces, update the existing roles, remove the role that is unassigned. On Ux, we need an option to unassign a assigned role. Perhaps a x, upon clicking it removes the role assignee value
